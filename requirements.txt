fredapi>=0.5.2
requests>=2.31
pandas>=2.0
numpy>=1.24
scipy>=1.11
statsmodels>=0.14
matplotlib>=3.7
seaborn>=0.13
jupyterlab>=4.0
openpyxl>=3.1
python-dotenv>=1.0
streamlit>=1.35.0
plotly>=5.22.0
scikit-learn>=1.4.0
xgboost>=2.0.0
# News & sentiment (positioning app): headline scoring runs on the built-in
# bond-oriented finance lexicon (stdlib) — no heavy ML deps, so it deploys on
# Streamlit Community Cloud. To use FinBERT instead, install `transformers` +
# CPU-only `torch` on a host with >=2GB RAM; the app auto-detects and upgrades.
